# systemd EnvironmentFile 格式，不要在等号两侧加空格。
# NFS 只负责传输；运行时使用板端持久化副本，重启后不依赖 NFS。
EDGEVISION_BUNDLE=/userdata/edgevision-gateway/edge-gateway-lite
EDGEVISION_STATE_DIR=/userdata/edgevision-gateway
# 使用独立版本库，避免把历史 oneshot 的 user_version=0 数据库误当成正式 schema。
# 旧 gateway.db 保留不动，需要导入时应使用显式、可审计的迁移工具。
EDGEVISION_DATABASE=/userdata/edgevision-gateway/edge-gateway-lite-v0.1.db
EDGEVISION_LOG=/userdata/edgevision-gateway/gateway.log

# v0.1 支持 simulated 或 stm32；正式板端默认使用真实 STM32 数据源。
EDGEVISION_SOURCE=stm32

# 健康检查用这些值核对真实设备节点；Gateway v0.1 内部默认值与之相同。
EDGEVISION_SERIAL=/dev/ttyS5
EDGEVISION_GPIOCHIP=/dev/gpiochip0
